---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['flex items-center gap-1', className]}>
  <span class="font-mono text-[12px] uppercase tracking-[0.08em] text-text-bright">
    ADORA<span class="text-teal-400">.</span>
  </span>
</div>
